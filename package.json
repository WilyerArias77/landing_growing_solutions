{
  "name": "growing-solutions-web",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Landing de Growing Solutions + chatbot de soporte (OpenAI) sobre funciones serverless de Vercel",
  "scripts": {
    "dev": "vercel dev",
    "check": "node --check api/chat.js && node --check api/_lib/prompt.js && node --check api/_lib/lead.js && node --check chat.js && node --check script.js"
  },
  "dependencies": {
    "openai": "^4.104.0"
  },
  "devDependencies": {
    "vercel": "^59.11.7"
  },
  "engines": {
    "node": ">=18"
  }
}
